/**
 * Scoring Configuration — re-export from @inboxkey/extraction-core
 *
 * Source of truth: packages/extraction-core/src/matching/scoring-config.ts
 * This file preserves existing `@/lib/matching/scoring-config` import paths
 * while keeping the actual logic in the shared package.
 */

export {
  DOMAIN_ALIASES,
  WATCH_SESSION_SCORING,
  CONFIDENCE_THRESHOLDS,
} from '@inboxkey/extraction-core'
