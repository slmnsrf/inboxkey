/**
 * Shared Account Components
 *
 * Foundation primitives for AccountsPanel redesign.
 * These components provide consistent patterns for:
 * - Stateful button interactions (loading, success, error states)
 * - Inline contextual feedback (replacing toasts)
 * - Account row layouts across providers
 * - Provider section headers
 */

export { StatefulButton } from './StatefulButton';
export { InlineAlert } from './InlineAlert';
export { AccountRow } from './AccountRow';
export { AccountSection } from './AccountSection';
